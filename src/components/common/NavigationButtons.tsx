"use client";

import ToolsNav from "@/components/common/ToolsNav";

/** @deprecated Use ToolsNav name — kept for layout imports. */
export default function NavigationButtons() {
  return <ToolsNav />;
}
